使用 Caddy 监听 `80` 和 `443` 端口并反向代理到本地应用的配置文件，默认配置自动申请 TLS 证书和重定向到 HTTPS。

如果需要作为静态文件服务器：

```
找到：
########
reverse_proxy @host 127.0.0.1:12345 # 反向代理到其他本地应用
########

然后改为：
########
root /home/user/www # 修改为自己存放 Web 文件的路径
########
```